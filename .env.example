# Environment Configuration for Tunnel API
# Copy this file to .env and adjust values

# Server
SERVER_PORT=8080
SERVER_HOST=0.0.0.0

# Database (PostgreSQL)
DATABASE_URL=postgres://tunnel:tunnel@localhost:5432/tunneldb?sslmode=disable

# JWT Configuration
JWT_SECRET=change-this-to-a-very-long-random-string-at-least-32-characters
JWT_ACCESS_TTL=60     # Access token TTL in minutes
JWT_REFRESH_TTL=7     # Refresh token TTL in days

# Tunnel Server (built-in, no external FRP needed)
TUNNEL_PORT=7001          # Client control port (VoidLink desktop connects here)
MC_PROXY_PORT=25565       # Shared Minecraft TCP proxy port
HTTP_PROXY_PORT=8081      # Shared HTTP proxy port (Dynmap/BlueMap)

# Tunnel Configuration
MIN_PORT=20000
MAX_PORT=20100
MAX_TUNNELS=3
DOMAIN=eu.yourdomain.com
REGION=eu

# SMTP Configuration (Optional - for Password Reset)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=user@example.com
SMTP_PASSWORD=your-smtp-password
SMTP_FROM=noreply@example.com
